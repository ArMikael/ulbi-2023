import React from 'react';
import SpButton from "./components/ui/sp-button/spButton";
import './styles.scss';

const App = () => {
    return (
        <div>
            React App
            <SpButton />
        </div>
    );
};

export default App;